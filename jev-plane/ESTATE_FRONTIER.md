# SZL Jev Plane — estate judgment, not another organ

Zoom-out across `szl-holdings` (126 GitHub repos), `SZLHOLDINGS` (Hub models / datasets / Spaces),
`a-11-oy.com` (product origin, Cloudflare → Space `a11oy`), and `a11oy.net` (proof origin).

This pack does **not** invent a fourth public origin. It does **not** let Jev ALLOW-alone.
It does **not** reimplement `szl-lambda-gate`, `szl-invariants`, or `szl-govsign`.

Jev supplies typed probabilities. Code owns routing, Λ, receipts, and side effects.

`plane/compose.py` turns answers into `{action, escalate, block_publish, reasons}` only.
It never signs, never merges, never updates Hub.

Λ remains Conjecture 1. Locked-proven remains exactly eight.
